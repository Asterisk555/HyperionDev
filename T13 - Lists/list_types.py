# Stores the namesof friends in a list, the order is  important because their ages are in the same order in friends_ages.
friends_names = ["Tai", "Soph", "Cal"]

# Prints first and last name using an f-string, and length of the list.
print (f"The first name on the list is {friends_names[0]}, the last name is {friends_names[-1]} and the total length of the list is {len(friends_names)}")

# Ages of friends in the same order they are listed in friends_names.
friends_ages = [ 16, 27, 45 ]